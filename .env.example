DATABASE_URL="postgresql://mediaguard:mediaguard@localhost:5432/mediaguard"
# MISTRAL_API_KEY=...   # For analysis and Mistral Voxtral STT (or send X-Mistral-API-Key header)
# ELEVENLABS_API_KEY=...  # For ElevenLabs STT token (when client does not provide key)
# LANGFUSE_SECRET_KEY=...  # For tracing (get from Langfuse UI after first login)
# LANGFUSE_PUBLIC_KEY=...  # Optional, for client-side
# LANGFUSE_BASE_URL=http://localhost:3100  # When using docker compose Langfuse (default: cloud)
